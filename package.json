{
  "name": "claude-web",
  "private": true,
  "workspaces": ["hub", "plugin"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "check": "bun run typecheck && bun run test",
    "start": "bun run --cwd hub start"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "bun-types": "1.4.2",
    "typescript": "^5.6.0"
  }
}